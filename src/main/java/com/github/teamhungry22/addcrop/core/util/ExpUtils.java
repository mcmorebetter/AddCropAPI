package com.github.teamhungry22.addcrop.core.util;

import com.github.teamhungry22.addcrop.api.AddCropNotFoundException;
import org.bukkit.entity.Player;

public class ExpUtils {
    public static void exp(Player player, String expName, double amount, String giveType, String expForm) {
        throw new AddCropNotFoundException();
    }

    private static void vanillaExp(Player player, Double amount, String giveType, String expForm) {
        throw new AddCropNotFoundException();
    }
}
