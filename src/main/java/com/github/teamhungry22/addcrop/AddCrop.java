package com.github.teamhungry22.addcrop;

import com.comphenix.protocol.ProtocolManager;
import com.github.teamhungry22.addcrop.api.AddCropAPI;
import com.github.teamhungry22.addcrop.api.AddCropNotFoundException;
import com.github.teamhungry22.addcrop.core.config.ConfigManager;
import com.github.teamhungry22.addcrop.core.line.LineManager;
import com.github.teamhungry22.addcrop.core.objects.action.ActionManager;
import com.github.teamhungry22.addcrop.core.objects.entity.EntityManager;
import com.github.teamhungry22.addcrop.core.objects.item.ItemManager;
import net.luckperms.api.LuckPerms;
import org.bukkit.plugin.PluginManager;
import org.bukkit.plugin.java.JavaPlugin;

public final class AddCrop extends JavaPlugin {
    @Override
    public void onLoad() {
        throw new AddCropNotFoundException();
    }

    @Override
    public void onEnable() {
        throw new AddCropNotFoundException();
    }

    @Override
    public void onDisable() {}

    private void loadManagers() {
        throw new AddCropNotFoundException();
    }

    private void loadEvents() {
        throw new AddCropNotFoundException();
    }

    public static AddCrop getInstance() {
        throw new AddCropNotFoundException();
    }

    public AddCropAPI getApi() {
        throw new AddCropNotFoundException();
    }

    public ProtocolManager getProtocolManager() {
        throw new AddCropNotFoundException();
    }

    public PluginManager getPluginManager() {
        throw new AddCropNotFoundException();
    }

    public ConfigManager getConfigManager() {
        throw new AddCropNotFoundException();
    }

    public ItemManager getItemManager() {
        throw new AddCropNotFoundException();
    }

    public EntityManager getEntityManager() {
        throw new AddCropNotFoundException();
    }

    public ActionManager getActionManager() {
        throw new AddCropNotFoundException();
    }

    public LineManager getLineManager() {
        throw new AddCropNotFoundException();
    }

    public LuckPerms getLuckPerms() {
        throw new AddCropNotFoundException();
    }

    public String getCustomItemPlugin() {
        throw new AddCropNotFoundException();
    }

    public String getServerVersion() {
        throw new AddCropNotFoundException();
    }

    public boolean isPAPI() {
        throw new AddCropNotFoundException();
    }

    public void setPAPI(boolean PAPI) {
        throw new AddCropNotFoundException();
    }

    public boolean isLoadData() {
        throw new AddCropNotFoundException();
    }

    public void setLoadData(boolean loadData) {
        throw new AddCropNotFoundException();
    }

    private boolean loadLuckPerms() {
        throw new AddCropNotFoundException();
    }

    private String loadCustomItemPlugin() {
        throw new AddCropNotFoundException();
    }

    private void stopPlugin(String text) {
        throw new AddCropNotFoundException();
    }
}
